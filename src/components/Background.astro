<div class="fixed inset-0 -z-10 overflow-hidden pointer-events-none">

    <!-- Top glow -->
    <div class="absolute -top-40 -left-40 w-[600px] h-[600px]
                bg-horizon-400/10
                rounded-full blur-3xl">
    </div>

    <!-- Bottom glow -->
    <div class="absolute bottom-[-200px] right-[-200px] w-[700px] h-[700px]
                bg-blue-500/10
                rounded-full blur-3xl">
    </div>

</div>

<div class="fixed inset-0 -z-10 pointer-events-none opacity-[0.03]
            bg-[url('/images/noise.png')] bg-repeat">
</div>