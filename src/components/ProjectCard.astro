---
const { title, description, image, link } = Astro.props;
---

<div class="bg-white rounded-2xl shadow shadow-horizon-300/50 hover:shadow-lg transition overflow-hidden">
  {image && (
    <img src={image} alt={title} class="w-full h-40 object-cover" />
  )}

  <div class="p-6">
    <h3 class="text-xl font-semibold mb-2">{title}</h3>
    <p class="text-gray-600 mb-4">{description}</p>
    <a href={link} class="text-blue-600 font-medium hover:underline">
      View Project →
    </a>
  </div>
</div>